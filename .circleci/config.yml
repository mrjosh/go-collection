# Golang CircleCI 2.0 configuration file
version: 2
jobs:
  build:
    docker:
      # specify the version
      - image: circleci/golang:1.10

    working_directory: /go/src/github.com/goshco/arrays
    steps:
      - checkout

      - run: go get github.com/stretchr/testify/assert

      # run tests
      - run: go test